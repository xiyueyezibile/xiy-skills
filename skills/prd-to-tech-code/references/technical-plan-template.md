# 技术方案模板

> 使用说明：根据信息来源、业务 LLM Wiki 和仓库实际情况填充本模板。技术方案必须能直接指导代码落位，详细到每个仓库的具体文件、锚点和代码职责；同时也要服务人阅读，保留必要的资料图、流程图、架构图、改造前后示意或 PRD/设计稿截图来源。可以删除确实不适用的小节，但不能省略会影响理解或实现正确性的业务规则、接口契约、状态分支、资料图、代码落位、风险和执行计划。

## 背景与目标

### 信息来源

| 来源 | 类型 | 可信度 | 关键结论 |
| --- | --- | --- | --- |
|  | PRD / 技术方案 / 聊天记录 / 文档 / 用户纠正 / 仓库证据 | 高 / 中 / 低 |  |

### 原始技术方案归一化

> 仅当输入包含已有技术方案、技术设计、改造方案、方案草稿或任务拆解时保留。目标是把原方案校验并补齐成当前模板，而不是直接照搬原方案落代码。

- 原始方案来源：
- 原始方案关键结论：
- 已被仓库代码证实的内容：
- 已被修正或废弃的内容：
- 模板补齐内容：业务归属 / 资料图 / 仓库证据 / 代码落位清单 / 风险 / 执行计划
- 仍需用户确认的问题：

### 业务知识库

- Wiki 根目录：`~/.prd-to-tech-code`
- 当前业务配置：`~/.prd-to-tech-code/config.json`
- Business slug：
- 业务归属判断：沿用当前业务 / 用户明确新业务 / 自动创建新业务 / 疑似新业务待确认
- 疑似新业务提示：
- 已读取页面：

### 资料图与参考材料

> 目标：让评审者快速理解改造背景、用户路径、系统边界和改造前后变化。资料可来自 PRD、设计稿、截图、聊天记录、其他文档、仓库已有文档或本方案生成的 Mermaid/ASCII 示意图。

| 材料 | 类型 | 来源 | 用途 | 是否纳入方案 |
| --- | --- | --- | --- | --- |
|  | PRD 截图 / 设计稿 / 流程图 / 架构图 / 时序图 / 改造前后示意 / 表格 / 其他 | 链接、文件路径、文档标题或截图说明 | 说明业务流程 / 页面变化 / 数据流 / 仓库边界 / 风险点 | 是 / 否 |

### 改造概览图

```mermaid
flowchart LR
  A[用户入口] --> B[页面/组件]
  B --> C[请求封装/服务]
  C --> D[后端/BFF/外部依赖]
```

### 改造前后对比

| 对比项 | 改造前 | 改造后 | 说明材料 |
| --- | --- | --- | --- |
|  |  |  |  |

### 仓库范围

| 仓库 | 本地路径 | 仓库职责 | 本次角色 | 关键入口 |
| --- | --- | --- | --- | --- |
|  |  | 前端 / 后端 / BFF / 组件库 / SDK / 配置 / 文档 | 需要修改 / 只读参考 / 待确认 |  |

### 背景

- 当前问题：
- 目标用户：

### 目标

- 业务目标：
- 用户价值：
- 成功标准：

### 非目标

- 本期不做：
- 后续迭代：

## 需求拆解

### 核心流程

1. 
2. 
3. 

### 功能范围

| 模块 | 本期能力 | 关键规则 | 是否必须 |
| --- | --- | --- | --- |
|  |  |  |  |

### 角色与权限

| 角色 | 可见入口 | 可执行操作 | 限制条件 |
| --- | --- | --- | --- |
|  |  |  |  |

## 当前系统理解

### 代码证据

| 仓库 | 代码位置 | 证据类型 | 事实摘要 | 对方案的影响 |
| --- | --- | --- | --- | --- |
|  |  | 路由 / 页面 / 组件 / 服务 / 类型 / 接口封装 / 权限 / 配置 / 数据流 / 业务规则 |  |  |

### 现有入口

- 页面/路由：
- 组件：
- 服务/接口：
- 配置：

### 现有数据流

```text
用户入口 -> 页面/组件 -> 请求封装 -> 服务/接口 -> 状态更新 -> UI反馈
```

### 可复用能力

- 可复用组件：
- 可复用接口：
- 可复用类型：
- 可复用工具：

## 技术设计

### 总体方案

- 实现方式：
- 模块边界：
- 关键取舍：

### 改动范围

| 仓库 | 文件/模块 | 改动内容 | 原因 |
| --- | --- | --- | --- |
|  |  |  |  |

### 代码落位清单

> 目标：让后续代码实现可以逐项照此落位。每一项必须具体到文件路径和现有锚点；如果还无法定位到文件级落点，继续阅读仓库代码或补充“待确认问题”，不要用抽象模块名代替。

| 序号 | 仓库 | 文件路径 | 现有锚点 | 改动类型 | 需要编写的代码 | 依赖/复用 | 输入输出 | 异常与状态处理 | 验证点 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  | 函数/组件/类型/配置/路由名 | 新增 / 修改 / 删除 | 写清要新增的函数、组件、类型、字段、分支、调用或样式职责 | 复用的工具、请求封装、组件、类型 | 入参、返回值、状态读写、事件参数 | Loading、空态、错误态、权限、降级、兼容 | 人工验证路径或关键断言 |

### 代码组织说明

- 新增文件：
- 修改文件：
- 删除文件：
- 复用现有能力：
- 需要保持兼容的旧逻辑：
- 不落代码但需要关注的只读证据：

### 跨仓库契约

| 上游仓库 | 下游仓库 | 契约 | 兼容要求 |
| --- | --- | --- | --- |
|  |  |  |  |

### 状态设计

| 状态 | 触发条件 | 用户可见表现 | 后续流转 |
| --- | --- | --- | --- |
|  |  |  |  |

## 数据与接口

### 数据模型

| 字段 | 类型 | 来源 | 是否必填 | 说明 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 接口契约

#### 接口名称

- 方法与路径：
- 请求参数：
- 响应结构：
- 错误码：
- 幂等/重试：
- 缓存/降级：

```ts
type Request = {
};

type Response = {
};
```

## 前端实现

### 页面与组件

- 入口展示：
- 组件拆分：
- 表单校验：
- Loading：
- 空态：
- 错误态：

### 交互规则

| 场景 | 用户操作 | 系统行为 | 反馈 |
| --- | --- | --- | --- |
|  |  |  |  |

### 埋点与监控

- 曝光：
- 点击：
- 提交成功：
- 提交失败：

## 后端实现

### 服务边界

- 责任范围：
- 上游依赖：
- 下游依赖：

### 核心逻辑

1. 
2. 
3. 

### 一致性与并发

- 事务：
- 幂等：
- 并发控制：
- 审计：

## 状态、异常与降级

| 异常场景 | 触发条件 | 处理方式 | 用户提示 | 是否阻塞 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 风险与取舍

| 风险 | 影响 | 规避方案 | 兜底方案 |
| --- | --- | --- | --- |
|  |  |  |  |

## 执行计划

1. 按代码落位清单处理类型、接口适配或数据转换
2. 按代码落位清单实现页面、组件或服务逻辑
3. 按代码落位清单处理 Loading、空态、错误态和异常分支
4. 补充必要配置、文档或迁移说明
5. 对照代码落位清单逐项确认人工验证路径

## 验证方式

- 本地运行：
- 人工验证路径：
- 关键用例：
- 未覆盖风险：
